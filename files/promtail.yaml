server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: REPLACEME

scrape_configs:
  - job_name: fcreplay
    pipeline_stages:
    static_configs:
    - targets:
        - localhost
      labels:
        job: fcreplay
        __path__: /home/fcrecorder/fcreplay/fcreplay.log